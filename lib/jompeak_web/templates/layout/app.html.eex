<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Jompeak · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header id="header_color">
      <section class="container">
        <nav role="navigation">
          <ul>
          <%= if @current_user do %>
            <li class = "nav_link"><%= @current_user.email %></li>
            <li class = "nav_link">|</li>
            <li><%= link "Sign out", to: Routes.session_path(@conn, :delete, @current_user), method: "delete", class: "nav_link"%></li>
          <% else %>
            <li><%= link "Register", to: Routes.user_path(@conn, :new), class: "nav_link" %></li>
            <li class = "nav_link">|</li>
            <li><%= link "Sign in", to: Routes.session_path(@conn, :new), class: "nav_link" %></li>
          <% end %>
          </ul>
        </nav>
          <img src="<%= Routes.static_path(@conn, "/images/jompeak_logo.png") %>" alt="Jompeak Logo"/>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
